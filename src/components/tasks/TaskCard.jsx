import { formatDate, isOverdue } from '../../utils/dateUtils';
export default function TaskCard({ item, onToggle, onEdit, onDelete }) { return <article className={`task-card ${item.status === 'Completed' ? 'done' : ''}`}><input type="checkbox" checked={item.status === 'Completed'} onChange={() => onToggle(item)} /><div className="task-body"><h3>{item.title}</h3><p>{item.description}</p><div className="card-line"><span className="soft-badge mint">{item.category}</span><span className={`priority ${item.priority.toLowerCase()}`}>{item.priority}</span><span className={isOverdue(item.dueDate, item.status === 'Completed') ? 'overdue' : 'muted'}>{formatDate(item.dueDate)}</span></div></div><div className="card-actions"><button onClick={() => onEdit(item)}>✎</button><button onClick={() => onDelete(item)}>×</button></div></article>; }
