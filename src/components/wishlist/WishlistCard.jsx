import { formatCurrency } from '../../utils/currencyUtils';
export default function WishlistCard({ item, onEdit, onDelete }) { return <article className="data-card wishlist-card"><div className="wishlist-image">{item.image ? <img src={item.image} alt="" onError={e => { e.currentTarget.style.display = 'none'; }} /> : <span>♡</span>}</div><div className="card-actions"><button onClick={() => onEdit(item)}>✎</button><button onClick={() => onDelete(item)}>×</button></div><div className="card-line"><span className={`priority ${item.priority.toLowerCase()}`}>{item.priority}</span><span className="muted">{item.category}</span></div><h3>{item.name}</h3><strong className="price">{formatCurrency(item.price)}</strong><div className="card-footer"><span className="soft-badge lavender">{item.status}</span>{item.link && <a href={item.link} target="_blank" rel="noreferrer">View link ↗</a>}</div></article>; }
