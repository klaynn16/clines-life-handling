import { formatDate } from '../../utils/dateUtils';
import { formatFileSize } from './PdfViewerModal';

export default function NoteCard({ item, onEdit, onDelete, onViewPdf, onDownloadPdf, onRemovePdf }) { return <article className="data-card note-card"><div className="card-actions"><button onClick={() => onEdit(item)} aria-label="Edit note">✎</button><button onClick={() => onDelete(item)} aria-label="Delete note">×</button></div><div className="note-pin">✦</div><span className="soft-badge yellow">{item.category}</span><h3>{item.title}</h3><p>{item.content}</p><small className="muted">Created {formatDate(item.createdAt)}</small>{item.pdf && <div className="pdf-preview"><div className="pdf-preview-icon">📄</div><div className="pdf-preview-info"><b>PDF</b><span title={item.pdf.filename}>{item.pdf.filename}</span><small>{formatFileSize(item.pdf.fileSize)}</small></div><div className="pdf-card-actions"><button onClick={() => onViewPdf(item.pdf)}>View PDF</button><button onClick={() => onDownloadPdf(item.pdf)}>Download</button><button className="pdf-remove" onClick={() => onRemovePdf(item)} aria-label="Remove PDF">×</button></div></div>}</article>; }
