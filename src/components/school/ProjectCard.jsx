import { formatDate } from '../../utils/dateUtils';
export default function ProjectCard({ item, onEdit, onDelete }) { return <article className="data-card project-card"><div className="card-actions"><button onClick={() => onEdit(item)}>✎</button><button onClick={() => onDelete(item)}>×</button></div><div className="card-line"><span className="soft-badge blue">Project</span><span className="muted">{item.subject}</span></div><h3>{item.name}</h3><p>{item.notes}</p><div className="progress-label"><span>Progress</span><b>{item.progress}%</b></div><div className="progress"><span style={{ width: `${item.progress}%` }}></span></div><div className="card-footer"><span>Due {formatDate(item.deadline)}</span><span className="muted">{item.status}</span></div></article>; }
