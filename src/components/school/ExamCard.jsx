import { formatDate } from '../../utils/dateUtils';
export default function ExamCard({ item, onEdit, onDelete }) { return <article className="data-card exam-card"><div className="card-actions"><button onClick={() => onEdit(item)}>✎</button><button onClick={() => onDelete(item)}>×</button></div><span className="icon-tile lavender">⌁</span><h3>{item.name}</h3><p>{item.subject}</p><div className="exam-date"><b>{formatDate(item.date, { month: 'short', day: 'numeric' })}</b><span>{item.time || 'Time TBD'}</span></div><small>{item.coverage || 'Coverage not added yet.'}</small></article>; }
