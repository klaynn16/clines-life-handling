export default function SubjectCard({ item, onEdit, onDelete }) { return <article className={`data-card subject-card ${item.color}`}><div className="card-actions"><button onClick={() => onEdit(item)} aria-label="Edit">✎</button><button onClick={() => onDelete(item)} aria-label="Delete">×</button></div><span className="subject-color"></span><h3>{item.name}</h3><p>{item.professor}</p><div className="card-meta"><span>◷ {item.schedule}</span><span>⌂ {item.room}</span></div></article>; }
