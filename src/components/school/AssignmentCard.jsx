import { formatDate, isOverdue } from '../../utils/dateUtils';
export default function AssignmentCard({ item, onEdit, onDelete, onComplete }) { return <article className={`data-card assignment-card ${item.status === 'Completed' ? 'completed' : ''}`}><div className="card-actions"><button onClick={() => onEdit(item)}>✎</button><button onClick={() => onDelete(item)}>×</button></div><div className="card-line"><span className={`priority ${item.priority.toLowerCase()}`}>{item.priority}</span><span className="muted">{item.subject}</span></div><h3>{item.title}</h3><p>{item.description}</p><div className="card-footer"><span className={isOverdue(item.dueDate, item.status === 'Completed') ? 'overdue' : ''}>Due {formatDate(item.dueDate)}</span><button className="link-button" onClick={() => onComplete(item)}>{item.status === 'Completed' ? 'Completed ✓' : 'Mark complete'}</button></div></article>; }
