import { formatDate } from '../../utils/dateUtils';
export default function GoalCard({ item, onEdit, onDelete }) { return <article className="data-card goal-card"><div className="card-actions"><button onClick={() => onEdit(item)}>✎</button><button onClick={() => onDelete(item)}>×</button></div><div className="goal-icon">◎</div><div className="card-line"><span className="soft-badge blue">{item.category}</span><span className="muted">{item.status}</span></div><h3>{item.name}</h3><p>{item.description}</p><div className="progress-label"><span>Progress</span><b>{item.progress}%</b></div><div className="progress"><span style={{ width: `${item.progress}%` }}></span></div><div className="card-footer"><span>Target {formatDate(item.targetDate)}</span><span>{item.progress >= 100 ? 'Completed ♡' : 'Keep going!'}</span></div></article>; }
