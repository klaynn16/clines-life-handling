export default function Header({ title, subtitle = 'Let’s make today productive.', onAdd }) { const now = new Date(); return <header className="topbar"><div><p className="eyebrow">{new Intl.DateTimeFormat('en-US', { weekday: 'long', month: 'long', day: 'numeric', year: 'numeric' }).format(now)}</p><h1>{title || 'Hi, Cline! ♡'}</h1><p className="muted">{subtitle}</p></div>{onAdd && <button className="button primary" onClick={onAdd}>＋ Add new</button>}</header>; }
