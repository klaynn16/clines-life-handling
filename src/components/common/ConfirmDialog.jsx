export default function ConfirmDialog({ title = 'Are you sure?', message, onCancel, onConfirm, confirmLabel = 'Delete' }) { return <div className="modal-backdrop"><div className="confirm-dialog" role="alertdialog" aria-modal="true"><span className="confirm-icon">!</span><h2>{title}</h2><p>{message}</p><div className="modal-actions"><button className="button ghost" onClick={onCancel}>Cancel</button><button className="button danger" onClick={onConfirm}>{confirmLabel}</button></div></div></div>; }
