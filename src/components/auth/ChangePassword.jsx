import { useState } from 'react';
import { createPasswordSecurity, verifyPassword } from '../../utils/passwordSecurity';
import { PasswordField } from './PasswordSetup';

export default function ChangePassword({ onChanged }) { const [current, setCurrent] = useState(''); const [next, setNext] = useState(''); const [confirm, setConfirm] = useState(''); const [show, setShow] = useState(false); const [error, setError] = useState(''); const [success, setSuccess] = useState(''); const submit = async e => { e.preventDefault(); setError(''); setSuccess(''); if (!(await verifyPassword(current))) return setError('Current password is incorrect.'); if (next.length < 6) return setError('New password must be at least 6 characters.'); if (next !== confirm) return setError('Passwords do not match.'); await createPasswordSecurity(next); setCurrent(''); setNext(''); setConfirm(''); setSuccess('Password changed successfully ♡'); onChanged?.(); }; return <form className="security-form" onSubmit={submit}><PasswordField label="Current password" value={current} onChange={setCurrent} show={show} setShow={setShow} /><PasswordField label="New password" value={next} onChange={setNext} show={show} setShow={setShow} /><PasswordField label="Confirm new password" value={confirm} onChange={setConfirm} show={show} setShow={setShow} />{error && <p className="form-error">{error}</p>}{success && <p className="form-success">{success}</p>}<button className="button primary" type="submit">Change password</button></form>; }
