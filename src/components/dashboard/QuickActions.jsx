export default function QuickActions({ onSelect }) { return <section className="quick-actions"><div><p className="eyebrow">SHORTCUTS</p><h2>Quick actions</h2></div><div className="quick-grid">{[['task','✓','Add task'], ['assignment','▦','Add assignment'], ['transaction','₱','Add transaction'], ['event','◷','Add event']].map(([key, icon, label]) => <button key={key} className="quick-button" onClick={() => onSelect(key)}><span>{icon}</span>{label}<b>＋</b></button>)}</div></section>; }
