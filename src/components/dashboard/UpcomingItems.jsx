import { formatDate } from '../../utils/dateUtils';
export default function UpcomingItems({ items }) { return <section className="panel"><div className="section-heading"><div><p className="eyebrow">NEXT UP</p><h2>Upcoming</h2></div><span className="soft-badge lavender">{items.length} items</span></div>{items.length ? <div className="upcoming-list">{items.map(item => <div className="upcoming-row" key={item.id}><span className={`item-dot ${item.tone || 'blue'}`}></span><div><b>{item.title}</b><small>{item.type} · {formatDate(item.date)}</small></div><span className="row-arrow">›</span></div>)}</div> : <p className="empty-inline">Your schedule is clear ♡</p>}</section>; }
