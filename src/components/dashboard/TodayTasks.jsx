import { formatDate } from '../../utils/dateUtils';
export default function TodayTasks({ tasks, onToggle }) { return <section className="panel"><div className="section-heading"><div><p className="eyebrow">FOCUS</p><h2>Today</h2></div><span className="soft-badge mint">{tasks.length} tasks</span></div>{tasks.length ? <div className="compact-list">{tasks.map(task => <label className={`compact-task ${task.status === 'Completed' ? 'done' : ''}`} key={task.id}><input type="checkbox" checked={task.status === 'Completed'} onChange={() => onToggle(task)} /><span><b>{task.title}</b><small>{task.category} · {task.priority} priority</small></span></label>)}</div> : <EmptyState text="Nothing planned for today ♡" />}</section>; }
function EmptyState({ text }) { return <p className="empty-inline">{text}</p>; }
