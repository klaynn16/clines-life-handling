import { useMemo } from 'react'; import { formatDate } from '../../utils/dateUtils';
export default function CalendarView({ events, onEdit, onDelete, monthDate, setMonthDate }) { const year = monthDate.getFullYear(); const month = monthDate.getMonth(); const days = new Date(year, month + 1, 0).getDate(); const start = new Date(year, month, 1).getDay(); const cells = useMemo(() => Array.from({ length: start + days }, (_, i) => i < start ? null : i - start + 1), [start, days]); const eventOn = day => events.filter(e => e.date === `${year}-${String(month + 1).padStart(2, '0')}-${String(day).padStart(2, '0')}`); return <section className="panel calendar-panel"><div className="calendar-toolbar"><button className="icon-button" onClick={() => setMonthDate(new Date(year, month - 1, 1))}>‹</button><h2>{new Intl.DateTimeFormat('en-US', { month: 'long', year: 'numeric' }).format(monthDate)}</h2><button className="icon-button" onClick={() => setMonthDate(new Date(year, month + 1, 1))}>›</button></div><div className="calendar-grid weekdays">{['Sun','Mon','Tue','Wed','Thu','Fri','Sat'].map(d => <b key={d}>{d}</b>)}</div><div className="calendar-grid">{cells.map((day, i) => <div className={`calendar-day ${day && day === new Date().getDate() && month === new Date().getMonth() && year === new Date().getFullYear() ? 'today' : ''}`} key={i}>{day && <><span>{day}</span>{eventOn(day).slice(0, 2).map(event => <button key={event.id} className={`calendar-event ${event.category.toLowerCase()}`} onClick={() => onEdit(event)}>{event.title}</button>)}{eventOn(day).length > 2 && <small>+{eventOn(day).length - 2} more</small>}</>}</div>)}</div>{events.length > 0 && <div className="calendar-agenda"><h3>All events</h3>{events.slice().sort((a, b) => a.date.localeCompare(b.date)).map(event => <div className="agenda-row" key={event.id}><span className={`item-dot ${event.category.toLowerCase()}`}></span><div><b>{event.title}</b><small>{formatDate(event.date)} · {event.category}</small></div><div className="card-actions"><button onClick={() => onEdit(event)}>✎</button><button onClick={() => onDelete(event)}>×</button></div></div>)}</div>}</section>; }
